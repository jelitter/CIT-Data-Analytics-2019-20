# Write a program that asks the user to enter a distance in kilometres and then converts
# that distance to miles(Miles=Kilometres * 0.6214).

kms = int(input("Enter distance in km: "))
miles = kms * 0.6214

print(f"{kms} km equals to {miles} miles")
